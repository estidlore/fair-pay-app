import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  actionButton: {
    marginRight: 8
  },
  formControl: {
    alignItems: "center",
    display: "flex",
    flexDirection: "row",
    marginVertical: 16
  },
  input: {
    width: "50%"
  },
  label: {
    fontSize: 16,
    width: "25%"
  }
});

export { styles };
