import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  formControl: {
    display: "flex",
    flexDirection: "row",
    marginBottom: 24
  },
  input: {
    width: "50%"
  },
  label: {
    fontSize: 16,
    width: "25%"
  }
});

export { styles };
